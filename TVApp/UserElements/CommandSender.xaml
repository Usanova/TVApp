<UserControl x:Class="TVApp.UserElements.CommandSender"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TVApp.UserElements"
             mc:Ignorable="d" d:Width="400" Height="30">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBox Text="{Binding CommandName, Mode=TwoWay}" Style="{StaticResource NonFocusTextBox}"></TextBox>
        <Button Command="{Binding ExecuteCommand}" Padding="5,0" Grid.Column="1" Style="{StaticResource NonFocusButton}">Вызвать команду</Button>
    </Grid>
</UserControl>
